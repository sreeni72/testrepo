pipeline {
    agent {
        label '!windows'
    }

      stages {
        stage('Build') {
         
            steps {
              
                echo 'Pulling... ' + (env.GIT_BRANCH).split("\\*/")[1]
                
               
            }
        }
    }
}
